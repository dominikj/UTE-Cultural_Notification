<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="pl.ute.culturaltip.activity.MainActivity">

    <Button
        android:id="@+id/close_add_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/phone_text_field"
        android:layout_alignStart="@+id/phone_text_field"
        android:layout_below="@+id/phone_text_field"
        android:layout_marginTop="32dp"
        android:text="@string/close_add" />

    <Button
        android:id="@+id/save_add_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/close_add_btn"
        android:layout_alignBottom="@+id/close_add_btn"
        android:layout_toEndOf="@+id/phone_label"
        android:layout_toRightOf="@+id/phone_label"
        android:text="@string/save_add" />

    <TextView
        android:id="@+id/name_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="Name" />

    <EditText
        android:id="@+id/name_text_field"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/name_label"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="14dp"
        android:ems="10"
        android:inputType="textPersonName" />

    <TextView
        android:id="@+id/phone_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/name_label"
        android:layout_alignStart="@+id/name_label"
        android:layout_below="@+id/name_text_field"
        android:layout_marginTop="42dp"
        android:text="Phone" />

    <EditText
        android:id="@+id/phone_text_field"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/name_text_field"
        android:layout_alignStart="@+id/name_text_field"
        android:layout_below="@+id/phone_label"
        android:layout_marginTop="19dp"
        android:ems="10"
        android:inputType="phone" />
</RelativeLayout>